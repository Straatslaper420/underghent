# Path to your Google service-account JSON key (gitignored)
# Use the same credentials.json as the main pipeline
GOOGLE_SHEETS_CREDENTIALS=../credentials.json

# Target spreadsheet (same one as the main pipeline)
GOOGLE_SPREADSHEET_ID=18ucjcKbxhJ88M-qboMeZRnMf4VX2EnWLlHhtyDkobms

# Tab name in the spreadsheet (do not change — the frontend reads this tab)
FEESTEN_WORKSHEET_NAME=feesten
